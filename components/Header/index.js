import HeaderNav from 'components/HeaderNav';
import Logo from 'components/Logo';

const Header = () => {
    return (
        <header>
            <Logo />
            <HeaderNav />
        </header>
    );
};

export default Header;
